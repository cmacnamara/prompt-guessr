# Production Environment Variables
# This file serves as a template for production deployment
# Copy to .env on EC2 instance and fill with actual values

# Node environment
NODE_ENV=production

# Server port
PORT=3001

# Redis connection (ElastiCache endpoint from Terraform)
REDIS_URL=redis://your-elasticache-endpoint.cache.amazonaws.com:6379

# CORS allowed origin (Amplify frontend URL)
CORS_ORIGIN=https://main.your-amplify-id.amplifyapp.com

# Image Generation Provider Configuration
# Production recommendation: huggingface (free tier available)
IMAGE_PROVIDER=huggingface

# Enable fallback to another provider if primary fails
ENABLE_FALLBACK=true

# Fallback provider (requires OPENAI_API_KEY)
FALLBACK_PROVIDER=openai

# Hugging Face Inference API
# Get your key at: https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# OpenAI API (Optional fallback)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# AWS S3 Configuration (for image storage)
# Set by Terraform/EC2 IAM role, but can override if needed
# S3_BUCKET_NAME=prompt-guessr-images-xxxxx
# AWS_REGION=us-east-1

# Logging
LOG_LEVEL=info
