# Backend Environment Variables
# Copy this to .env and fill in your actual values

# Node environment
NODE_ENV=development

# Server port
PORT=3001

# Redis connection
REDIS_URL=redis://localhost:6379

# Image Generation Provider Configuration
# Available providers: mock, huggingface, openai
# Default: mock (free placeholder images for development)
IMAGE_PROVIDER=mock

# Enable fallback to another provider if primary fails
# Default: false
ENABLE_FALLBACK=false

# Fallback provider (only used if ENABLE_FALLBACK=true)
# Options: huggingface, openai
# Example production config: IMAGE_PROVIDER=huggingface, ENABLE_FALLBACK=true, FALLBACK_PROVIDER=openai
FALLBACK_PROVIDER=openai

# Hugging Face Inference API (Free tier available)
# Get your key at: https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=your_huggingface_api_key_here

# OpenAI API (Paid service)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# CORS allowed origin (frontend URL)
CORS_ORIGIN=http://localhost:3000
